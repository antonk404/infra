apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: jitsi-prosody-config
  namespace: {{ .Values.namespace }}
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.prosody.storage.size }}
