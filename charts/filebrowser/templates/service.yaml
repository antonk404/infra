apiVersion: v1
kind: Service
metadata:
  name: filebrowser
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: filebrowser
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
