apiVersion: v1
kind: Service
metadata:
  name: element
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: element
  ports:
    - port: {{ .Values.element.service.port }}
      targetPort: {{ .Values.element.service.port }}
