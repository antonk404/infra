apiVersion: v1
kind: Service
metadata:
  name: stepik-solver
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: stepik-solver
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
